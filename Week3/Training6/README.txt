프로그램 실행 시 각 사분면에 4개의 삼각형을 생성합니다.
각 삼각형의 색은 생성시 랜덤으로 정해집니다.

삼각형의 세 정점이 벽과 충돌하는지 체크해 collision vector를 구하고,
결과에 따라 방향과 회전각을 변경합니다.
회전은 쿼터니온을 사용합니다.
a를 누르면 내부가 채워진 삼각형을 그리고, b를 누르면 외곽선만 있는
삼각형을 그립니다.

삼각형의 크기는 size_multiplier로 정해지는데, 0.5에서 1.5 사이의 값이며,
새 삼각형 생성 시마다 변화하며, 임계값에 도달하면 변화 방향을 바꿉니다.